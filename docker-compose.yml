services:
  laravel_frankenphp:
    container_name: laravel_frankenphp
    image: image_laravel_frankenphp
    build:
      dockerfile: Dockerfile
      context: .
    ports:
      - 80:8000
    env_file:
      - .env
